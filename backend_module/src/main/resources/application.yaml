spring:
  application:
    name: backend_module
  datasource:
    url: jdbc:postgresql://localhost:5433/laba_j200
    username: postgres
    password: root
  jpa:
    show-sql: true
server:
  port: 8080
